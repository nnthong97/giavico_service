spring:
  application:
    name: beverage-formulation-engine
  datasource:
    url: ${SPRING_DATASOURCE_URL:jdbc:mysql://localhost:3306/giavico_formulation?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC}
    username: ${SPRING_DATASOURCE_USERNAME:giavico}
    password: ${SPRING_DATASOURCE_PASSWORD:giavico}
  jpa:
    hibernate:
      ddl-auto: update
    open-in-view: false
    properties:
      hibernate:
        format_sql: true
  jackson:
    default-property-inclusion: non_null

server:
  port: ${SERVER_PORT:18181}
  shutdown: graceful

giavico:
  cors:
    allowed-origins: ${GIAVICO_CORS_ALLOWED_ORIGINS:http://localhost:4200}

management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics
  endpoint:
    health:
      probes:
        enabled: true

ollama:
  base-url: ${OLLAMA_BASE_URL:http://localhost:11434}
  generate-path: ${OLLAMA_GENERATE_PATH:/api/generate}
  model: ${OLLAMA_MODEL:gemma4}
  timeout: ${OLLAMA_TIMEOUT:PT5M}
  connect-timeout: ${OLLAMA_CONNECT_TIMEOUT:PT10S}
